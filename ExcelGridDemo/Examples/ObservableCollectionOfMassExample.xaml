<Window x:Class="ExcelGridDemo.ObservableCollectionOfMassExample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="http://schemas.galador.net/xaml/spreadsheet"
        xmlns:dataGridDemo="clr-namespace:ExcelGridDemo"
        Title="ObservableCollection&lt;Mass&gt;" Height="450" Width="300">
    <Grid>
        <!--<p:DataGrid ItemsSource="{Binding Items}" CellDefinitionFactory="{Binding CellDefinitionFactory}"/>-->
        <p:DataGrid ItemsSource="{Binding Items}" CellDefinitionFactory="{Binding CellDefinitionFactory}">
            <p:DataGrid.ColumnDefinitions>
                <p:ColumnDefinition PropertyName="" Header="Mass" />
            </p:DataGrid.ColumnDefinitions>
        </p:DataGrid>
    </Grid>
</Window>
